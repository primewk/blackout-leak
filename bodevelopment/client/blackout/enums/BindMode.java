package bodevelopment.client.blackout.enums;

public enum BindMode {
   Toggle,
   Pressed;

   // $FF: synthetic method
   private static BindMode[] $values() {
      return new BindMode[]{Toggle, Pressed};
   }
}
